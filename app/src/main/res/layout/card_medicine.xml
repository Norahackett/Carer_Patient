<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="medicine"
            type="ie.wit.carerpatient.models.CarerPatientModel" />

        <variable
            name="reportViewModel"
            type="ie.wit.carerpatient.ui.report.ReportViewModel" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:elevation="24dp">

        <RelativeLayout
            android:id="@+id/cardMedicine"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <ImageView
                android:id="@+id/imageIcon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:layout_alignParentTop="true"
                android:layout_marginEnd="16dp"
                android:contentDescription="@string/app_name"
                app:srcCompat="@mipmap/ic_launcher_round" />

            <TextView
                android:id="@+id/medicineName"
                android:layout_width="114dp"
                android:layout_height="56dp"
                android:layout_alignParentTop="true"
                android:layout_marginStart="9dp"
                android:layout_marginTop="8dp"
                android:layout_toEndOf="@+id/imageIcon"
                android:text="@{medicine.name}"
                android:textColor="@color/black"
                android:textSize="24sp"
                tools:text="Medicine_name" />


            <TextView
                android:id="@+id/quantity"
                android:layout_width="wrap_content"
                android:layout_height="90dp"
                android:layout_alignParentTop="true"
                android:layout_marginStart="25dp"
                android:layout_marginTop="11dp"
                android:layout_toEndOf="@+id/medicineName"
                android:text="@{(Integer.toString(medicine.amount) + medicine.type) }"
                android:textColor="@color/black"
                android:textSize="24sp"
                tools:text="@string/quantity" />

            <TextView
                android:id="@+id/type"
                android:layout_width="wrap_content"
                android:layout_height="82dp"
                android:layout_alignParentTop="true"
                android:layout_marginStart="266dp"
                android:layout_marginTop="27dp"
                android:layout_toEndOf="@+id/imageIcon"
                android:text="@{medicine.type}"
                android:textColor="@color/black"
                android:textSize="24sp"
                tools:text="Type" />

            <TextView
                android:id="@+id/time"
                android:layout_width="wrap_content"
                android:layout_height="82dp"
                android:layout_alignParentTop="true"
                android:layout_marginStart="44dp"
                android:layout_marginTop="110dp"
                android:layout_toEndOf="@+id/imageIcon"
                android:text="@{@string/formatTime2(medicine.time)}"
                android:textColor="@color/black"
                android:textSize="24sp"
                tools:text="02:09pm"/>


        </RelativeLayout>
    </androidx.cardview.widget.CardView>
</layout>
